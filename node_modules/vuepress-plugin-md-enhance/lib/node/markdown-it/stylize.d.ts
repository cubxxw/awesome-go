/**
 * 覆盖 inline rule, 包括
 *
 * - 对渲染标签增加属性
 * - 对文本进行替换
 */
import type { PluginWithOptions } from "markdown-it";
import type { StylizeOptions } from "../../types";
export interface StylizeFrontmatter {
    /**
     * Content to be skipped in this page
     */
    noStylize?: string[];
}
export interface StylizeMarkdownEnv {
    frontmatter: StylizeFrontmatter;
}
export declare const stylize: PluginWithOptions<StylizeOptions>;
