import { loadScript } from "./utils";
const state = {};
export const useState = () => state;
export const loadReact = (code) => Promise.all([
    loadScript(state, code.babel),
    loadScript(state, code.react),
    loadScript(state, code.reactDOM),
]);
export const loadVue = (code) => {
    const promises = [loadScript(state, code.vue)];
    if (code.useBabel)
        promises.push(loadScript(state, code.babel));
    return Promise.all(promises);
};
export const loadNormal = (code) => code.useBabel ? loadScript(state, code.babel) : Promise.resolve();
//# sourceMappingURL=loadScript.js.map